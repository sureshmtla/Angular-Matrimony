import { EcreateDirective } from './ecreate.directive';

describe('EcreateDirective', () => {
  it('should create an instance', () => {
    const directive = new EcreateDirective();
    expect(directive).toBeTruthy();
  });
});
