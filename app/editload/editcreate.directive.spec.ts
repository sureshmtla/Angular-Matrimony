import { EditcreateDirective } from './editcreate.directive';

describe('EditcreateDirective', () => {
  it('should create an instance', () => {
    const directive = new EditcreateDirective();
    expect(directive).toBeTruthy();
  });
});
